version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: novelExchangeApi
      POSTGRES_USER: novelExchangeApi
      POSTGRES_PASSWORD: G@7fT3xP#z9R!dV2
    ports:
      - "5432:5432"
    volumes:
      - data:/var/lib/postgresql/data

volumes:
  data:
